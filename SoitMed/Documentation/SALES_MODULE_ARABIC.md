# 📊 وحدة المبيعات ونظام تتبع الأنشطة - الوثائق العربية

## نظرة عامة
نظام شامل لتتبع أنشطة المبيعات وتقييم الأداء مصمم خصيصاً لشركة SoitMed. يوفر النظام إمكانيات متقدمة لتسجيل الأنشطة، إدارة الصفقات والعروض، وتحليلات مفصلة للمديرين.

## 🏗️ الهيكل المعماري

### 1. نماذج البيانات (Data Models)

#### التعدادات (Enums)
```csharp
public enum InteractionType { Visit, FollowUp }           // نوع التفاعل
public enum ClientType { A, B, C, D }                    // تصنيف العميل
public enum ActivityResult { Interested, NotInterested }  // نتيجة النشاط
public enum RejectionReason { Cash, Price, Need, Other }  // سبب الرفض
public enum DealStatus { Pending, Won, Lost }             // حالة الصفقة
public enum OfferStatus { Draft, Sent, Accepted, Rejected } // حالة العرض
```

#### الكيانات الرئيسية
- **ActivityLog**: سجل الأنشطة اليومية
- **Deal**: الصفقات التجارية
- **Offer**: العروض التجارية

### 2. طبقة الخدمات (Services)

#### ActivityService
- إنشاء الأنشطة مع المعاملات الآمنة
- إدارة الصفقات والعروض
- التحقق من الصلاحيات

#### ManagerDashboardService
- إحصائيات شاملة للمديرين
- تحليل الأداء
- تقارير مفصلة

### 3. واجهات برمجة التطبيقات (APIs)

#### أنشطة المبيعات
- `POST /api/activities/tasks/{taskId}/activities` - إنشاء نشاط جديد
- `GET /api/activities` - الحصول على أنشطة المستخدم
- `GET /api/activities/{id}` - الحصول على نشاط محدد

#### إدارة الصفقات
- `PUT /api/deals/{id}` - تحديث الصفقة

#### إدارة العروض
- `PUT /api/offers/{id}` - تحديث العرض

#### لوحة تحكم المدير
- `GET /api/manager/dashboard-stats` - إحصائيات المدير

## 📋 دليل الاستخدام

### 1. إنشاء نشاط جديد

#### مثال: زيارة عميل مع صفقة
```json
POST /api/activities/tasks/1/activities
{
    "interactionType": "Visit",
    "clientType": "A",
    "result": "Interested",
    "comment": "العميل أبدى اهتماماً قوياً بجهاز الرنين المغناطيسي",
    "dealInfo": {
        "dealValue": 750000.00,
        "expectedCloseDate": "2024-02-15"
    }
}
```

#### مثال: متابعة مع عرض
```json
POST /api/activities/tasks/1/activities
{
    "interactionType": "FollowUp",
    "clientType": "B",
    "result": "Interested",
    "comment": "العميل طلب عرض مفصل للمعدات الطبية",
    "offerInfo": {
        "offerDetails": "عرض شامل لمعدات الأشعة والرنين المغناطيسي",
        "documentUrl": "https://example.com/proposal.pdf"
    }
}
```

#### مثال: رفض العميل
```json
POST /api/activities/tasks/1/activities
{
    "interactionType": "Visit",
    "clientType": "C",
    "result": "NotInterested",
    "reason": "Price",
    "comment": "العميل وجد السعر مرتفعاً جداً"
}
```

### 2. تحديث الصفقة

```json
PUT /api/deals/1
{
    "status": "Won",
    "dealValue": 800000.00,
    "expectedCloseDate": "2024-02-20"
}
```

### 3. تحديث العرض

```json
PUT /api/offers/1
{
    "status": "Accepted",
    "offerDetails": "تم قبول العرض مع تعديلات طفيفة"
}
```

### 4. إحصائيات المدير

```json
GET /api/manager/dashboard-stats?startDate=2024-01-01&endDate=2024-01-31
```

#### الاستجابة المتوقعة:
```json
{
    "success": true,
    "data": {
        "startDate": "2024-01-01",
        "endDate": "2024-01-31",
        "totalActivities": 150,
        "totalVisits": 100,
        "totalFollowUps": 50,
        "totalDeals": 25,
        "totalOffers": 30,
        "wonDeals": 15,
        "lostDeals": 8,
        "pendingDeals": 2,
        "acceptedOffers": 20,
        "rejectedOffers": 8,
        "draftOffers": 2,
        "totalDealValue": 5000000.00,
        "wonDealValue": 3000000.00,
        "averageDealValue": 200000.00,
        "conversionRate": 16.67,
        "offerAcceptanceRate": 66.67,
        "clientTypeStats": [
            {
                "clientType": "A",
                "clientTypeName": "A",
                "count": 60,
                "percentage": 40.0,
                "totalValue": 3000000.00
            }
        ],
        "salespersonStats": [
            {
                "userId": "user1",
                "userName": "أحمد محمد",
                "totalActivities": 75,
                "totalDeals": 12,
                "wonDeals": 8,
                "totalValue": 1500000.00,
                "wonValue": 1000000.00,
                "conversionRate": 16.0
            }
        ],
        "recentActivities": [
            {
                "id": 1,
                "userName": "أحمد محمد",
                "interactionType": "Visit",
                "interactionTypeName": "Visit",
                "clientType": "A",
                "clientTypeName": "A",
                "result": "Interested",
                "resultName": "Interested",
                "comment": "زيارة ناجحة لمستشفى 57357",
                "createdAt": "2024-01-30T10:00:00Z",
                "dealValue": 500000.00
            }
        ]
    }
}
```

## 🔐 نظام الصلاحيات

### الأدوار المطلوبة
- **Salesman**: يمكنه إنشاء وتحديث الأنشطة والصفقات والعروض
- **SalesManager**: يمكنه الوصول لإحصائيات المدير
- **SuperAdmin**: صلاحيات كاملة

### التحقق من الصلاحيات
- كل مستخدم يمكنه فقط الوصول لأنشطته الخاصة
- المديرون يمكنهم رؤية إحصائيات فريقهم
- التحقق من ملكية المهام قبل إنشاء الأنشطة

## ✅ الاختبارات

### اختبارات الوحدة (Unit Tests)
- اختبارات شاملة لجميع الخدمات
- اختبارات التحكم في الأخطاء
- اختبارات التحقق من الصلاحيات
- اختبارات المعاملات الآمنة

### اختبارات التكامل (Integration Tests)
- اختبارات واجهات برمجة التطبيقات
- اختبارات قاعدة البيانات
- اختبارات الأداء

## 🚀 التطبيق والإعداد

### 1. إنشاء قاعدة البيانات
```bash
dotnet ef migrations add AddSalesFunnelEntities
dotnet ef database update
```

### 2. تشغيل الاختبارات
```bash
dotnet test SoitMed.Tests
```

### 3. تشغيل التطبيق
```bash
dotnet run
```

## 📊 مؤشرات الأداء الرئيسية (KPIs)

### للمندوبين
- عدد الأنشطة اليومية
- معدل التحويل (Conversion Rate)
- قيمة الصفقات المربوحة
- عدد العروض المقبولة

### للمديرين
- أداء الفريق الإجمالي
- توزيع العملاء حسب التصنيف
- تحليل الاتجاهات الشهرية
- مقارنة أداء المندوبين

## 🔧 استكشاف الأخطاء

### الأخطاء الشائعة
1. **خطأ الصلاحيات**: تأكد من أن المستخدم له الدور الصحيح
2. **خطأ المهمة**: تأكد من أن المهمة تنتمي للمستخدم
3. **خطأ التحقق**: تأكد من صحة البيانات المرسلة

### سجلات الأخطاء
- جميع الأخطاء مسجلة في نظام السجلات
- يمكن تتبع الأخطاء من خلال معرف النشاط
- إشعارات فورية للأخطاء الحرجة

## 📞 الدعم الفني

للحصول على الدعم الفني أو الإبلاغ عن مشاكل:
- البريد الإلكتروني: support@soitmed.com
- الهاتف: +20-123-456-7890
- ساعات العمل: 9:00 ص - 6:00 م (بتوقيت القاهرة)

---

**تم تطوير هذا النظام بواسطة فريق التطوير في SoitMed**  
**آخر تحديث: يناير 2024**



